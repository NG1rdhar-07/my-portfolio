# Environment variables for the portfolio project
# Copy this file to .env and fill in values as needed.
#
# NOTE: The @lovable.dev/vite-tanstack-config exposes VITE_* vars to the
# client at build time. Never prefix secrets with VITE_ unless they are
# intentionally public.

# No environment variables are required for the base portfolio.
# If you add API integrations (e.g. Supabase, Stripe), add them here.
